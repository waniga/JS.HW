max(); // undefined
max(2); // 2
max(3, 1); // 3
max(7, 3, 9, 2); // 9



function max(a,b,c,d){
    if(a === undifined){
        return undifined;
    }

    //ใส่แค่ a
    if(b === undifined)
    return a

    //ใส่แค่ a,b
    if(a === undifined){
        return a > b ? : b
    }
//ใส่แค่ a,b,c
    if(a === undifined){
    return Math.max(a,b,c)
}


}

//console.log(max())
//console.log(max(9))
//console.log(max(5,200))